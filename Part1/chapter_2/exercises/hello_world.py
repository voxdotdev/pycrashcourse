# Sets variable message to equal string, which is then printed to the terminal

message = "Hello Python world!"
print(message)

# Overwrites the previously stored string in message and replaces it with the one below
# because Python is top bottom execution the first line with be the former and the string provided below as the second line.

message = "Hello Python Crash Course world!"
print(message)

# see notes.md for variable rules / guidelines