## Use range() function to skip odd numbers 
# Starts with the value 2, then adds 2 to the value. 
# adds 2 repeatedly until it reaches or passes the end value, 11.

even_numbers = list(range(2, 11, 2))
print(even_numbers)